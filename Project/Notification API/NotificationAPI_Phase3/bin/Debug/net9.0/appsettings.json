{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning",
            "NotificationAPI_Phase3": "Information"
        }
    },
    "AllowedHosts": "*",


    // RabbitMQ connection settings
    // Must match exactly what Order.API uses
    // Both services connect to the same RabbitMQ instance
    "RabbitMQ": {
        "Host": "localhost",
        "Port": "5672",
        "Username": "guest",
        "Password": "guest"
    }
}
