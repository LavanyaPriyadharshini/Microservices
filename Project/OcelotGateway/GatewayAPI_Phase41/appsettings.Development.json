{
  // ════════════════════════════════════════════════════════════════════════
  // DEVELOPMENT ENVIRONMENT SETTINGS
  // ════════════════════════════════════════════════════════════════════════
  // This file is loaded ONLY in Development environment
  // It overrides settings from appsettings.json

  "Logging": {
    "LogLevel": {
      // More verbose logging in development for debugging
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      // Enable detailed Ocelot logging for troubleshooting routes
      "Ocelot": "Debug"
    }
  }
}
